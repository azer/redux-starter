{
  "name": "{kik:name}",
  "version": "1.0.0",
  "description": "{kik:redux:desc}",
  "main": "index.js",
  "dependencies": {
    "react": "^0.14.1",
    "react-dom": "^0.14.1",
    "react-redux": "^4.0.0",
    "redux": "^3.0.4",
    "redux-thunk": "^1.0.0"
  },
  "devDependencies": {
    "babelify": "^6.1.3",
    "browserify": "^12.0.1",
    "uglify-js": "^2.5.0",
    "watchify": "^3.6.0"
  },
  "scripts": {
    "build": "browserify index.js | uglifyjs -cm > public/bundle.js",
    "watch": "watchify index.js -o public/bundle.js --debug --verbose",
    "serve": "cd public && python -m SimpleHTTPServer 8765"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "repository": {
    "url": "{kik:remote}",
    "type": "git"
  },
  "author": "{kik:github}"
}
