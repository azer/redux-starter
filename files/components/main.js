import React, { Component } from 'react';

class {kik:className} extends Component {
  render() {
    return (
      <div>
        {this.props.example}
        <button onClick={this.props.incr}>increment</button>
      </div>
    );
  }
}

export default {kik:className};
